{
  "name": "pandemic",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "nyc mocha --retries 3",
    "coverage": "nyc report --reporter=text-lcov | coveralls && nyc report --reporter=text-lcov | codeclimate-test-reporter",
    "watch": "mocha -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.0",
    "codeclimate-test-reporter": "^0.5.0",
    "coveralls": "^2.13.1",
    "mocha": "^3.4.2",
    "nyc": "^11.1.0",
    "standard": "^10.0.2",
    "stryker": "^0.11.2",
    "stryker-api": "^0.9.0",
    "stryker-html-reporter": "^0.9.1",
    "stryker-mocha-framework": "^0.5.0",
    "stryker-mocha-runner": "^0.8.1"
  },
  "nyc": {
    "reporter": [
      "html",
      "text-summary"
    ]
  },
  "standard": {
    "env": {
      "mocha": true
    },
    "globals": [
      "it",
      "describe"
    ]
  }
}
